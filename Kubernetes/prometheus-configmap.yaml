apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: fcg
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s

    scrape_configs:
      - job_name: catalog-api
        metrics_path: /metrics
        static_configs:
          - targets: ["catalog-api.fcg.svc.cluster.local:80"]

      - job_name: users-api
        metrics_path: /metrics
        static_configs:
          - targets: ["users-api.fcg.svc.cluster.local:80"]
