apiVersion: v1
kind: Service
metadata:
  name: konga
  namespace: fcg
spec:
  selector:
    app: konga
  ports:
    - name: http
      port: 1337
      targetPort: http
      nodePort: 31337
  type: NodePort
