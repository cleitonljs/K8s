apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api-deployment
  namespace: fcg

spec:
  replicas: 1

  selector:
    matchLabels:
      app: users-api

  template:
    metadata:
      labels:
        app: users-api

    spec:
      containers:
        - name: users-api
          image: users-api:latest

          imagePullPolicy: Never

          ports:
            - containerPort: 8080

          env:

            # =========================================
            # CONFIG MAP
            # =========================================
            - name: ASPNETCORE_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: users-api-configmap
                  key: ASPNETCORE_ENVIRONMENT

            - name: RabbitMQ__Host
              valueFrom:
                configMapKeyRef:
                  name: users-api-configmap
                  key: RabbitMQ__Host

            - name: RabbitMQ__Port
              valueFrom:
                configMapKeyRef:
                  name: users-api-configmap
                  key: RabbitMQ__Port

            - name: RabbitMQ__Queues__FGC
              valueFrom:
                configMapKeyRef:
                  name: users-api-configmap
                  key: RabbitMQ__Queues__FGC            

            # =========================================
            # SECRET
            # =========================================
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: users-api-secret
                  key: ConnectionStrings__DefaultConnection

            - name: RabbitMQ__Username
              valueFrom:
                secretKeyRef:
                  name: users-api-secret
                  key: RabbitMQ__Username

            - name: RabbitMQ__Password
              valueFrom:
                secretKeyRef:
                  name: users-api-secret
                  key: RabbitMQ__Password